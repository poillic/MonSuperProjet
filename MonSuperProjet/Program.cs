using System;

namespace MonSuperProjet
{
    class Program
    {
        static void Main( string[] args )
        {
            int age = 10;
            float time = 10.3f;
            string name = "Toto";
            bool isOk = false;

            Console.WriteLine( $"La valeur de la variable age est {age}." );
            Console.WriteLine( $"La valeur de la variable time est {time}." );
            Console.WriteLine( $"La valeur de la variable name est {name}." );
            Console.WriteLine( $"La valeur de la variable isOk est {isOk}." );
        }
    }
}
